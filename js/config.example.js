// Copie este arquivo para `config.js` e preencha com as chaves do seu projeto Supabase.
// (Project Settings → API). A chave "anon" é pública por design — a proteção dos dados
// vem das políticas de Row Level Security definidas em supabase/schema.sql.
//
// Enquanto estiver vazio, o app roda em MODO DEMO (dados só no navegador, via localStorage).
export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
